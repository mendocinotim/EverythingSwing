# Core dependencies
torch==2.3.0
numpy==1.24.0  # Satisfies both numba and NeMo requirements
python-dotenv==1.1.0
pydantic==2.11.3

# Audio processing
librosa==0.10.2  # Updated to meet NeMo's requirement
soundfile==0.12.1

# NeMo Toolkit with ASR components only
nemo-toolkit[asr]==2.2.1 --extra-index-url https://pypi.nvidia.com

# Compatibility
protobuf==3.20.3
ruamel.yaml==0.18.10
